export const toSeconds = (metric: number) => Math.round(metric)/1000

export const toMilliseconds = (metric: number) => Math.round(metric)

export const toPercentage = (metric: number) => Math.round(metric * 100)
